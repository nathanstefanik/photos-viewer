# Immich Read-Only Display - Environment Configuration
# 
# IMPORTANT: Copy this file to .env and fill in your values
#            cp .env.example .env
#
# For Docker: The .env file should be in the project root directory
# The docker-compose will automatically read it

# =============================================================================
# Immich Connection (REQUIRED - Application will not start without these)
# =============================================================================

# Your Immich server URL (without trailing slash)
# For Docker on same host: use host.docker.internal instead of localhost
# Examples:
#   - http://192.168.1.100:2283  (LAN IP - recommended)
#   - http://host.docker.internal:2283  (Docker on same machine)
#   - https://photos.example.com  (if using HTTPS)
IMMICH_URL=http://192.168.1.100:2283

# Your Immich API key (KEEP THIS SECRET!)
# Generate one in Immich: User Settings → API Keys → New API Key
# Use a dedicated read-only user for better security
IMMICH_API_KEY=your-api-key-here

# =============================================================================
# Server Settings
# =============================================================================

# Host to bind to (use 0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Port to run on
PORT=8000

# Enable debug mode (shows API docs at /api/docs)
# WARNING: Set to false in production!
DEBUG=false

# =============================================================================
# CORS Settings (Security)
# =============================================================================

# Allowed origins for CORS (comma-separated)
# In production, set this to your actual frontend domain only
# Example: https://gallery.example.com
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# =============================================================================
# Cache Settings
# =============================================================================

# Cache TTL for people list (seconds) - default 5 minutes
CACHE_TTL_PEOPLE=300

# Cache TTL for search suggestions (seconds) - default 10 minutes
CACHE_TTL_SUGGESTIONS=600
